<Grid Margin="0,10,6,0">
${@IF|person|
<StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
<Path Margin="8,0" Width="18" Height="18" Fill="{DynamicResource ColorBrush5}"
                    Stretch="Uniform"
                    Data="M100 100 L300 100 L250 300 L50 300 Z "/>
<TextBlock Text="By ${person}" FontSize="14" VerticalAlignment="Center" Foreground="{DynamicResource ColorBrush5}"/>
</StackPanel>
}
<StackPanel Orientation="Horizontal" VerticalAlignment="Center">
<local:MyIconTextButton Text="启动" ToolTip="快速启动游戏" EventType="启动游戏"
    EventData="1.20.4" LogoScale="0.85" Logo="{StaticResource LaunchIcon}" />

</StackPanel>
</Grid>